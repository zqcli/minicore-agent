# Agent Evidence Subset

See [the acceptance report](../followups.md). `provenance.json` records the
accepted source/artifacts and paired test totals; `FILES.sha256` verifies this
Agent-focused evidence subset. The complete paired TUI, Session, stream, failure
and cleanup evidence is in sibling `minicore-tui/docs/verification/followups/`.
Artifact/source archives remain in the local accepted stash and are not duplicated
in Git. Raw evidence is marked `-text` to preserve byte hashes across checkouts.
