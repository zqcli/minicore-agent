# MiniCore Agent 0.5.0 Freeze Verification

**Status:** measured; local source freeze (unreleased).

This is the central record for the 0.5.0 local source freeze. It records the
source lineage, measured parent gates, scoped smoke checks, compatibility
accounting, and remaining execution limits. It is not a release announcement.
See [Remote Gates](#remote-gates) and [Static Checks](#static-checks) for the
measured status surfaces.

## Scope And Status

- Current source: `8a581a86dad7490cf5b7f21c54782d6e86623aca`.
- Local `dev` was fast-forwarded to `12a4530`, then S1-S3 were committed on
  `dev`. The local `refactor/0917-nonbreaking-simplify` branch was deleted; remote refs remain
  untouched. `origin/dev` remains
  `66f183470bd9c5bdb359b1c7666381135898861f`, and the remote simplification ref
  remains `12a4530fb67fb923002fc88b2fba12f06e9270c5`.
- No 0.5.0 tag, published artifact, installation, or push is recorded.
- S4 changed documentation only. No production source, test, dependency, CI,
  or Runtime file was changed by S4.

## Provenance

| Checkpoint | Commit | Meaning |
|---|---|---|
| Historical baseline | `12a4530` | Prior nonbreaking simplification record and all-gate evidence for the 0.3.3-era source. |
| S1 | `ad4eace` | Agent package version bump to `0.5.0` in the package manifest and lock entry. |
| S2 | `7d29983` | Historical specifications, context, plans, release notes, and navigation archive. |
| S3 | `8a581a8` | Current 0.5.0 guides, RPC documentation, crate rustdoc, and example comment. |

The detailed simplification record remains [historical evidence](0917-simplify.md).
Its prior gates are retained as 0.3.3-era evidence and are not relabeled as
0.5.0 results. Historical bodies, artifact facts, and limitations remain
unchanged.

## Compatibility Accounting

- `Cargo.toml` package `minicore-agent`: `0.5.0`.
- `Cargo.lock` package `minicore-agent`: `0.5.0`.
- Runtime dependency: `minicore-runtime 0.4.1` at
  `6cd2bdbc634437dea925495c61c7eb0be10ba171`.
- RPC protocol version: `1`.
- Session, Tool-record, and summary storage format versions: `1`, `1`, and `1`.
- The scoped `12a4530..8a581a8` diff over `src`, `tests`, `Cargo.toml`, and
  `Cargo.lock` shows only the package/lock version change plus `src/lib.rs`
  rustdoc; no test files changed. The public API, dependency graph, Runtime pin,
  RPC/event schemas, and storage schemas are unchanged in this freeze delta.
- The current Profile contract excludes executable `subagent`; this record does
  not claim that every behavior since 0.3.3 was unchanged.

## Remote Gates

Parent fresh-source runs used:

- source: `/root/minicore-agent-050/freeze-source`
- stable target: `/root/minicore-agent-050/target-final`
- MSRV target: `/root/minicore-agent-050/target-msrv`
- logs: `/root/minicore-agent-050/logs`
- toolchain: `rustc/cargo 1.97.1`; MSRV: `1.85.0`

| Gate | Result | Log |
|---|---|---|
| `cargo fmt --all -- --check` | `PASS` | `freeze-fmt.log` |
| stable `cargo check --locked --all-targets` | `PASS` | `freeze-check.log` |
| stable `cargo test --locked --all-targets` | `756 passed / 0 failed / 2 existing ignored` | `freeze-tests.log` |
| stable `cargo test --locked --doc` | `PASS`: 1 doctest compiled; `no_run` was not executed | `freeze-doctest.log` |
| strict `cargo clippy --locked --all-targets -- -D warnings` | `PASS` | `freeze-clippy.log` |
| `RUSTDOCFLAGS=-Dwarnings cargo doc --locked --no-deps` | `PASS` | `freeze-rustdoc.log` |
| MSRV `1.85.0` all-target tests | `756 passed / 0 failed / 2 existing ignored` | `freeze-msrv.log` |
| MSRV `1.85.0` doctest | `PASS`: 1 doctest compiled | `freeze-msrv-doctest.log` |
| Windows GNU all-target check | `PASS`: compile-only | `freeze-windows.log` |
| macOS all-target check | `PASS`: compile-only | `freeze-macos.log` |

The Windows check retained the same five existing test-only warnings: unused
imports/`start_entered` in `src/agent/tests.rs`, runtime-gate functions in
`src/sessions.rs`, and `write_reload_config` in integration tests. This was a
check gate, not Clippy; the warnings did not lower the result.

The macOS check used `/root/.local/bin/zig` `0.13.0` with
`CRATE_CC_NO_DEFAULTS=1` and
`CC_x86_64_apple_darwin='/root/.local/bin/zig cc -target x86_64-macos'`.
The historical 0917 macOS blocker remains unchanged; this fresh check succeeded
because the usable Zig binary was outside the prior PATH.

Before the final fresh run, the completed 0917 targets occupied `4.7+3GiB` and
were cleaned; the S1 target was also cleaned before creating the final fresh
stable/MSRV targets. The pinned Runtime checkout was not modified.

Native macOS/Windows execution, hosted CI, and Live Provider tests were not run.
They remain outside these measured compile-only and remote gate results.

## Scoped Smoke

The public example configuration and a synthetic `OPENAI_API_KEY` were used in
a temporary directory. The smoke invoked only `agent.ping` and shutdown; it did
not make a model request.

- `freeze-cli.log`: `minicore-agent 0.5.0`.
- `freeze-smoke.jsonl`: ping version `0.5.0`, protocol `1`, the same 13
  capabilities, shutdown `ok`, and stdout containing only JSON frames.
- The remote host lacked `jq`; the downloaded JSONL was validated locally with
  `jq` (`true`). This is an evidence-tool detail, not an Agent failure.
- Downloaded evidence is retained outside the repository at
  `/tmp/minicore-agent-050-evidence`.

## Static Checks

These checks ran locally without Cargo against the current worktree.

- Markdown files scanned: `32`.
- Relative links with a file path checked: `88`; missing targets: `0`.
- Anchors checked: `3` (including two same-file links); missing anchors: `0`.
- RPC methods in `dispatch_inner`: `33`.
- RPC methods in `canonical_method`: `33`.
- RPC methods in the documentation index: `33`; missing/extra: `0`/`0`.
- Package version in manifest/lock: `0.5.0`/`0.5.0`.
- Runtime version/revision: `0.4.1` /
  `6cd2bdbc634437dea925495c61c7eb0be10ba171`.
- RPC and storage format constants: protocol `1`; Session/Tool-record/summary
  `1`/`1`/`1`.
- Current Markdown JSON blocks: `46` total and `46` parseable (`33` illustrative
  placeholder examples, `13` without placeholders); non-illustrative parse
  failures: `0`.
- Current-guide `0.3.3` identity leaks: `0`; historical release links remain
  intentional.
- `git diff --cached --check`: `PASS`.

JSON examples containing placeholders such as `ses_...`, `lup_...`, or `...`
are illustrative templates rather than literal runtime requests. Their values
are not wire evidence, and no protocol field is changed to make an example
literal JSON.

## Record Boundaries

This record centralizes measured status so current guides do not repeat gate
counts. It does not modify historical verification records, logs, JSON results,
checksums, native captures, or other artifacts. The current documentation is
reachable from [README](../../README.md#documentation-and-evidence), and the
full current contract remains [the RPC guide](../rpc.md).

The measured result is a local source freeze for an unreleased package. It does
not assert a tag, push, publication, installation, or broader native/provider
execution than the evidence listed above.
